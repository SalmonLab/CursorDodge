# CursorDodge 開発用 AGENTS（Windows 専用）

このリポジトリは Windows 向けの常駐型ユーティリティ `CursorDodge` の実装リポジトリです。
目的は、クリック直後に文字入力を開始した際にカーソルが邪魔になる問題を避けるため、設定可能な方向・距離・速度でカーソルを一時的にずらすことです。

## 優先順位
1. 安全性（入力フック・管理者権限・終了手段）
2. 動作の確実性（常駐状態、クリック/入力検知）
3. 設定の再現性（保存と再読込）
4. 変更の局所化

## 変更方針
- 変更は `CursorDodge` の本体機能（フック、設定、トレイ、起動設定）に限定する。
- 不要なリファクタリングやスタイル刷新はしない。
- 公開済みの設定キー名・JSON 形式は極力維持し、後方互換を崩さない。

## 開発・検証ルール
- 実装前に `README.md` とこの `AGENTS.md` を確認する。
- 依存は .NET 標準 API と既存参照外部パッケージを増やさない。
- Windows 固有の挙動は対象 OS で動作確認を優先する。
- 可能なら最小限の検証を行い、結果を記録する。
  - `dotnet build`
  - `dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true`
- UI 変更時はトレイアイコン・設定画面の主要項目が表示・保存できることを確認する。

## アーキテクチャ上の前提
- 対象フレームワーク: `.NET 8.0-windows`
- 主構成: WinForms (`OutputType=WinExe`)
- 低レベルフック: `GlobalHooks.cs`（マウス・キーボード）
- 設定保存: `%AppData%\CursorDodge\settings.json`（`AppSettings.cs`）
- 常駐トレイUI: `CursorDodgeContext.cs`
- 配布: `CursorDodge.exe`（Single File 可）

## 実装時の禁止事項
- コンソールを起動しない（例: WinExe のまま維持する）。
- 既存コマンドを削除して既定動作を壊す変更を避ける。
- 自動起動レジストリ書き換えは明示的にユーザー操作された経路（コンテキストメニュー経由）でのみ行う。
- クリック判定・キー判定ロジックと移動アニメーションを分離し、可読性と保守性を保つ。

## リリース・配布ルール
- 配布は原則 `self-contained` 単体実行で作成する。
- 署名、インストーラ、更新機構の追加は別タスクで計画し、必要最小の範囲に留める。

## 例外ケース
- 日本語UI文字列の表記ゆれが発生する場合は、表示文字列を一貫させる（`無効化`/`有効化`、`保存` など）。
- 方向角の規則は「上方向を 0°、右方向を 90°」で固定する。

## 既知の検証結果テンプレート（必要なら追記）
- 変更内容:
- 検証コマンド:
- 実行日:
- 問題/例外:
